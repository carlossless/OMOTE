#pragma once

#include <string>
#include <stdint.h>

extern std::string scene_name_receiver;

extern uint16_t SCENE_RECEIVER;
extern uint16_t SCENE_RECEIVER_FORCE;

void register_scene_receiver(void);
